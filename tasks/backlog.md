# Backlog

The living backlog. Update this file directly as items are picked up,
finished, or superseded — don't create a new dated snapshot for routine
changes. `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` is an older,
dated snapshot with its own detailed root-cause analysis (FR-01…FR-20); it
stays as reference and is linked below rather than duplicated. Items are
never deleted here — mark them `done` (with the commit) instead, so the file
still explains what was decided and why.

**Priority key:**
- **P0** — before real customers are forwarded to this line. Call forwarding
  is currently OFF, so nothing here is an active outage, but these are the
  gate for turning it on.
- **P1** — next. Correctness/safety issues on the current Live path, or work
  Aryan has already approved.
- **P2** — soon. Quality and lower-risk correctness.
- **P3** — later. Nice-to-have, deferred, or speculative.

Status values: `open`, `in progress` (name the stream), `status: verify`
(listed here because an older doc flagged it, but nobody has confirmed
against current code whether it's still true), `done` (link the commit).

---

## P0

| Item | Why | Source | Status |
| --- | --- | --- | --- |
| Revert `OWNER_TRANSFER_MODE` and `TRANSFER_WINDOW_END` to their normal values after Aryan finishes his transfer-fail test | These are temporary production overrides (`OWNER_TRANSFER_MODE=real`, `TRANSFER_WINDOW_END=23:00`) that ring Richa's actual phone outside the normal window. Fine while forwarding is off and a test is pending; wrong to leave in place afterward. | `docs/SESSION_HANDOFF_2026-09-15.md`, Aryan 2026-09-16 | open |
| Decide the `PHOREST_WRITE_MODE=real` / forwarding-off boundary before flipping forwarding on | Test bookings currently hit the real calendar. That's intentional for the taste test; it needs an explicit "we're ready" checkpoint before real customers reach this line, not an implicit one. | `docs/SESSION_HANDOFF_2026-09-15.md` | status: verify |

## P1

| Item | Why | Source | Status |
| --- | --- | --- | --- |
| Context diet — one-page CURRENT state, archived log, generated symbol map, per-area briefing files, lessons digest | Every agent pays ~30k+ tokens just to get oriented (state.md ≈96k tokens, twilioStream.ts ≈81k). Files, not caches, carry knowledge between sessions; make them cheap to read. Splitting twilioStream.ts by concern is the bigger follow-up and rides with Realtime retirement Stage 1. | Aryan 2026-09-16, `state.md` | done (2026-09-16, commits 60593d8 2c80d4f 643ff53; split twilioStream.ts remains open under Realtime Stage 1) |
| Expose a build identity (git commit sha) on `GET /admin/voice-test` | `engine:"live"` proves the branch, not the commit. After 2026-09-16 every deploy from the live worktree looks identical on that route; a sha makes "did the new build ship?" a one-line check instead of a Railway-dashboard read. | `docs/CODEMAP.md` Deploying, this session | open |
| Visit tools had no handler on the Live engine (advertised, banned-then-unbanned in the prompt, but registered Realtime-only) | Erica told a real caller she could not check a combined opening. Fixed with a contract test that every advertised tool has a handler on both engines. | `state.md` workstream H | done (workstream H, commit `ea8bdc3`) |
| Realtime retirement, Stage 1 — narrow `VOICE_ENGINE` to `live` only, delete the 26 `voiceEngine` branches in `twilioStream.ts`, extract computed facts out of `buildInstructions()` into a shared facts module | Aryan retired the Realtime engine for good on 2026-09-16. Stage 1 is the load-bearing rewrite; see the plan for exact file:line detail, verification, and rollback per stage. | `docs/REALTIME_RETIREMENT_PLAN_2026-09-16.md`, Aryan 2026-09-16 | open |
| Realtime retirement, Stage 2 — delete `OpenAIRealtimeSession`, its test file, and the 6 Realtime-only scripts (after extracting the shared types it also exports) | Same decision as above. Depends on Stage 1 landing first. | `docs/REALTIME_RETIREMENT_PLAN_2026-09-16.md` | open |
| Realtime retirement, Stage 3 — rewrite `CLAUDE.md`'s voice/session-validation rules and the `docs/GPT-SOL/` folder, which currently describe Realtime as the live architecture | Same decision. `CLAUDE.md`'s voice rule is already wrong today (Live hardcodes `marin` in `liveSession.ts`, not env-tunable) — worth fixing regardless of retirement pace. | `docs/REALTIME_RETIREMENT_PLAN_2026-09-16.md` | open |
| Visit tools accept ONE service; retire the `prepare_appointment_action` / `confirm_appointment_action` pair | The model currently has to decide "one service → proposal pair, two-plus → visit tool," and that decision is what it gets wrong in practice (register, Sept 10 3:17: moved threading, discovered tint, moved threading again). Letting `book_visit`/`reschedule_visit`/`cancel_visit` accept a single service removes the branch entirely: 14 tools become 12. Tool defs at `twilioStream.ts:993` (`prepare_appointment_action`) / `:1011` (`confirm_appointment_action`); handlers registered at `:1898`/`:1901` (line numbers as of 2026-09-16 20:20 ET — this file is under concurrent edit, re-grep before acting). | `docs/AUDIT_REVIEW_2026-09-15.md` §5, Aryan approved for backlog 2026-09-16 | open |
| Same-client duplicate-booking guard using the warmed prefetch appointments | The fresh re-check before every booking write protects the *slot*, not the *intent* — nothing stops a client from booking the same service twice in one call if the calendar happens to have room both times. `this.prefetch.appointments` (populated at `twilioStream.ts:2149`, invalidated on writes at `:4113`, `:4884`, `:4968`; read at `:5483`–`:5485`) already holds the caller's upcoming visits; a guard reading "this client already has this service on this date — confirm they want a second one" is cheap and turns a data property back into an explicit rule. | `docs/AUDIT_REVIEW_2026-09-15.md` §7 area / open items, Aryan approved 2026-09-16 | open |
| `cancel_visit` needs a read-back planning phase, matching `book_visit`/`reschedule_visit`'s plan → confirm shape | Flagging here only because it's the direct ancestor of the "visit tools accept one service" item above — re-check whether that item's shape needs adjusting now that all three visit tools share the plan→confirm contract. | `docs/AUDIT_REVIEW_2026-09-15.md` §4 | done (workstream C, commit `2812dd5`) |
| SMS taste test from Aryan's handset → decide `OWNER_SMS_MODE=real` (also un-silences voice owner notifications) → `SMS_OPEN_TO_ALL=true` + `SMS_OWNER_PHONE` → Richa | The SMS concierge is live for Aryan's number only; the taste test in the handoff doc is the gate before widening it to the real client base and handing owner escalations to Richa. | `docs/SMS_CONCIERGE_HANDOFF.md` 2026-09-16 | open |

## P2

| Item | Why | Source | Status |
| --- | --- | --- | --- |
| `log_running_late`'s note should name the service, not just the time | Every appointment in a sitting currently gets the same sentence when one runs late; Richa's FYI doesn't say which service. `noteText` built around `twilioStream.ts:5612`ish (inside `handleLogRunningLate`, starts `:5575`). | `docs/PROMPT_AUDIT_2026-09-15.md` §4.4 | open |
| Mirror QA check in the call-review routine: completed action with no "anything else" offer and no caller goodbye before hangup | Register item 14 already catches the opposite failure (asking "anything else?" *after* an explicit goodbye). The mirror — Erica going silent after finishing a booking/change/cancellation/message, with neither an offer nor a goodbye before the line closes — was not yet a named check. Added in this session; see `.claude/commands/call-review.md`. | `docs/AUDIT_REVIEW_2026-09-15.md` §3, this session (workstream G) | done (this session) |
| Item 05's deeper half — one shared service resolver, not "two selectors" | `get_prices` and `suggest_availability` both call `resolveService`; they only differ in what they do with `notOffered`. The register's "unify the selectors" framing was a misdiagnosis — there's one selector already. The real defect (conjunctions/filler words winning a match, e.g. "brow and lip" → wax) is fixed. | `docs/AUDIT_REVIEW_2026-09-15.md` §2.3, §7 | done (workstream A, commits `96e1624`, `9dbc13d`) |
| Register item 15 — narration ("Okay, checking that", "Okay, rescheduling for you now") | The Live prompt already forbids narrating tools/reasoning; this is a voice-model compliance gap, not a prompt conflict. `5769300`'s fix is confirmed NOT in the deployed backend prompt. Measure via the QA sweep rather than another prompt rewrite; consider a note on the slower tools ("reply with the answer only") if it recurs. | `docs/PROMPT_AUDIT_2026-09-15.md` §4.1, `docs/SESSION_HANDOFF_2026-09-15.md` | open |
| FR-16 (2026-09-02 backlog) — exact closing-minute boundary treated as available | Hours boundary is inclusive when it should be exclusive; a service starting exactly at close can be offered when it shouldn't fit. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P2 table | status: verify |
| FR-17 (2026-09-02 backlog) — cancellation/reschedule FYI texts can omit the original appointment time | Makes multiple appointments ambiguous in Richa's texts; fix is to always include salon-local original (and, for reschedule, new) date/time from server-owned state. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P2 table | status: verify — may be partly superseded by the 2026-09-15 visit-tools work; re-check against current SMS templates before starting |
| FR-19 (2026-09-02 backlog) — occasional no-op preambles ("let me check") with no tool call | Tighten the existing no-fake-wait rule; ear-test rather than adding scenario scripts. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P2 table | status: verify |
| FR-20 (2026-09-02 backlog) — a broad daypart preference ("afternoon") can still prompt for an exact time | Prompt already says to use the daypart; it doesn't always hold. Accept an explicit daypart/window value server-side instead of relying on prompt compliance alone. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P2 table | status: verify |
| Transcription hint staging (FR-18 / older todo.md P0 item) — `language:'en'` + salon-vocabulary `prompt` behind env, to reduce "Richa" → "Rich/Richard" mis-transcription | Was accepted live once (2026-09-01 probe) but never flipped on with a byte-identical-when-unset snapshot test guarding it. Also relevant to the OPENAI_INPUT_TRANSCRIPTION default-off convention documented in `tasks/lessons.md`. | `tasks/todo.md` (2026-09-01 plan, P0), `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` FR-18 | status: verify |
| Outbound SMS campaigns (Phase 2) — gated on cross-system ≤1 msg/48–72h cap and A2P description update | Phorest's native rebooking SMS is already ON, so any Erica-initiated outbound campaign needs a cross-system send cap plus a broadened A2P campaign description (currently "asking for Google reviews") before it can launch. | `docs/SMS_CONCIERGE_2026-09-14.md` Phase 2/3 | open |
| status: verify — does Twilio's built-in Advanced Opt-Out also auto-reply to STOP on this number, making Erica's own STOP confirmation a duplicate? | If Twilio's carrier-level opt-out already answers STOP, `smsCompliance.ts`'s `STOP_REPLY` could be sending the client two confirmations for one STOP. | `docs/areas/sms-concierge.md`, this session | status: verify |
| status: verify — escalation reply gate: when the model says "checking with Richa" but did not call `escalate_to_owner`, nothing is sent to the owner (seen once in the 2026-09-16 local smoke while Phorest was unreachable) | The client hears a promise the owner never sees, because the escalation text is only sent from inside the tool handler, not from the model's prose. | `src/services/smsAgent.ts`, 2026-09-16 local smoke | status: verify |

## P3

| Item | Why | Source | Status |
| --- | --- | --- | --- |
| Enable `@typescript-eslint/no-non-null-assertion`; remove the 30 `!` sites in `twilioStream.ts` | Classified in the audit review: most are safe-by-construction, but one (`canonicalNames.get(date)!` ~`:3492` at the time of that audit) silently weakens a gate on a missing name instead of crashing loudly. Mechanical once classified. Being actively worked today (workstream F). | `docs/AUDIT_REVIEW_2026-09-15.md` §6 | in progress (workstream F) |
| Staff matcher: length-scaled edit-distance bound + gate on `closest` + shrink stopwords to real function words | Fixes the root cause more precisely than the current stopword-list workaround; being worked today (workstream D). | `docs/AUDIT_REVIEW_2026-09-15.md` §2.1–2.2 | done (workstream D, commit `39c0f95`) |
| Closing mechanics: server counts the "anything else" offer so "never twice" stops being prose-only; reword the visit tools' success notes | Being worked today (workstream E), alongside the call-review mirror check above. | `docs/AUDIT_REVIEW_2026-09-15.md` §3 | done (workstream E, commit `fa37bca`) |
| FR-12–FR-15 (2026-09-02 backlog) — Realtime response-state-machine races (double `response.create`, stale marks, clipped goodbye audio, error-state races) | All four are written against the **Realtime** response lifecycle (`response.created`, terminal events, marks keyed to `responsePart`). Live's protocol is a different shape entirely (continuous audio, no `response.create` collision in the same sense) — these may be entirely moot once Realtime retires, or may have a Live-shaped analog nobody has checked for. Don't port the fixes as-is; re-derive against Live's actual protocol if anyone chases these. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P1 table | status: verify — likely moot post-retirement, needs a fresh look either way |
| FR-01, FR-03, FR-05, FR-06, FR-08, FR-09, FR-10, FR-11 (2026-09-02 backlog) — remaining concurrency/write-safety items: closed-date fail-open on API error, duplicate client creation, uncertain-write retry duplicating a booking, `end_call` racing an in-flight write, transfer-reason-as-message leakage, terminal SMS status mishandling, cancelled-appointment reschedule, appointment-ownership drift | None of these were referenced anywhere in `state.md` after the 2026-09-02 doc was written — no evidence either that they were fixed or that they're still live bugs. Every one is plausible against the current write-guard code in `twilioStream.ts`, but none has been re-verified against today's handlers. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P0 table | status: verify |
| FR-02 (2026-09-02 backlog) — concurrent booking/reschedule/cancel calls in one call, including duplicate FYI notifications | Possibly superseded by the 2026-09-15 visit-tools work (`book_visit`/`reschedule_visit`/`cancel_visit` execute sequentially through existing handlers with the same write guards) — but that work covers the *deliberate* multi-service case, not necessarily two genuinely concurrent tool calls from one model turn. Needs a direct check, not an assumption. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P0 table | status: verify |
| FR-04 (2026-09-02 backlog) — duplicate `call_id` / semantically identical calls performing the same write twice | Same caveat as FR-02 — check against current tool-call dedup behavior before assuming open or closed. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P0 table | status: verify |
| FR-07 (2026-09-02 backlog) — the fatal-error failover path can dial Richa during an away period | This is now explicitly an **owner policy decision**, not a bug: the current temporary config (`OWNER_TRANSFER_MODE=real`) makes this deliberate for the test window. Revisit once the P0 revert item above is done — if Aryan wants "never dial while away" absolute, `failoverToOwner()` (`twilioStream.ts:6829`) needs a vacation-aware branch. | `docs/FUNCTIONAL_RELIABILITY_BACKLOG_2026-09-02.md` P0 table, `docs/REALTIME_RETIREMENT_PLAN_2026-09-16.md` §d.1 | status: verify |
| Owner action: rotate/verify OpenAI + Phorest keys, purge stale secrets from tracked history (`PLAN.md`, old `NextSteps.md`) | Long-standing item from the 2026-07-18 audit pass; never confirmed done in `state.md`. | `tasks/todo.md` Phase 0 | status: verify |
| Endpoint security hardening: Twilio signature validation on `/twilio/voice`, HMAC-auth the `/twilio/stream` WS, delete any remaining unauthenticated REST writes, rate limiting | Same 2026-07-18 audit; foundational once real customer traffic is forwarded here (ties to the P0 forwarding gate above). | `tasks/todo.md` Phase 1 | status: verify |
| Tool-layer write-path safety: ownership guard on cancel/reschedule ids, slot validation against offered times, Zod `safeParse` at every handler, client-phone-index TTL refresh | Same audit; some of this may already be covered by later 2026-08/09 work (fresh-check-before-write, `TOOL_SCHEMAS` in `toolSchemas.ts`) — needs a check against current code, not a re-implementation from scratch. | `tasks/todo.md` Phase 2 | status: verify |
| SMS layer: booking-confirmation text, missed-call text-back, 24–48h reminders (verify Phorest doesn't already send these) | Market-research-backed feature gap; unclear if superseded by the SMS Concierge work referenced in `docs/SMS_CONCIERGE_2026-09-14.md`/`SMS_CONCIERGE_HANDOFF.md` — check those docs before treating this as still open. | `tasks/todo.md` Phase 4.2 | status: verify |
| Owner daily digest content check — does it already cover "revenue booked while closed"? | `services/digest.ts` exists per `docs/CODEMAP.md`; unclear if it covers everything Phase 4.3 asked for. | `tasks/todo.md` Phase 4.3 | status: verify |
| Spam/robocall gate before opening a session; Spanish language support; in-call soft upsell; voicemail fallback; waitlist | Market-gap features, no evidence of implementation in `state.md`. Lower priority than anything touching write safety or the Realtime retirement. | `tasks/todo.md` Phase 4.5–4.9 | open |
| Greeting nudge so callers don't freeze up talking to an AI, without lengthening the mandatory recording-disclosure line | Explicitly deferred by Aryan until the prompt rework had soaked in production (2026-08-26). Given the GPT-Live migration is now the prompt rework, this may be ready to revisit. | `tasks/todo.md` "Nice-to-have backlog" | open |
| Spam/telemarketer lookup via Twilio Lookup + Nomorobo Spam Score add-on | Deferred by Aryan 2026-08-26, requires the owner to install a Twilio Marketplace add-on first. | `tasks/todo.md` "Nice-to-have backlog" | open — blocked on owner action |
| Pronunciation hearing-hints for "Richa" (Richard, Rich, Raja, Recharge, "the judge") | Old item, P2 in the 2026-09-01 prompt audit plan; unclear if superseded by the staff-matcher stopword work. | `tasks/todo.md` P2 | status: verify |
| Verbosity table + digit-by-digit phone read-back | Old item from the 2026-09-01 prompt audit plan. | `tasks/todo.md` P2 | open |
| Flow states (Goal / How to respond / Exit when) for BOOK, RESCHEDULE, CANCEL, LATE | Old item from the 2026-09-01 prompt audit plan; the Live prompt has since gained a lot of this shape informally through tool-result notes — check for overlap before starting. | `tasks/todo.md` P2 | status: verify |
| C6/C8/C9/C10/C12/C13/C11 prompt polish items (one-failure rule, staff-note preamble apology, never-invent-a-reason, identity wording, variety line, no repeated clarification, constraint-word trim) | Old grab-bag from the 2026-09-01 prompt audit; several may already be covered by later rewrites (SERVE/backend rules). Needs a fresh read against the current backend rules before treating any single one as still open. | `tasks/todo.md` P2 | status: verify |
| Pre-warm next-open-day availability for "your usual" service; service-history-based offers | Prefetch-side feature, identity-gated, offer-not-assume. Needs a Railway-latency measurement first per the original note. | `tasks/todo.md` P2 | open |
| Semantic VAD listening test (staging only) | Explicitly not recommended in the 2026-09-01 audit at the time; may be moot for Live (different turn-detection model entirely) — re-evaluate only if Realtime retirement raises questions about Live's own turn-taking tuning. | `tasks/todo.md` P2 | status: verify — likely superseded |
| Existing Tony Stark placeholder client record cleanup | Explicit customer-data edit, needs Aryan's ok per the permission model; not part of any release so far. | `tasks/todo.md` (2026-09-02 release block) | open — blocked on owner action |
| Provider-level schedule/absence state before multi-provider onboarding | `business.json.vacations` is salon-wide only today; fine for one provider, wrong the moment a second stylist joins. No urgency while the salon has one provider. | `tasks/todo.md` (2026-09-02 release block) | open — not urgent |

## Added 2026-09-17 — from the call-QA fix run (`525233d`…`994ce3d`) and its review gate

| Item | Why | Where | Status |
| --- | --- | --- | --- |
| **P1 — `failoverToOwner` has no `transferFailback` gate** (`twilioStream.ts` ~7152). Any fatal error on a failback segment inside the transfer window RE-DIALS the phone that just rang out. Review-gate finding F7; **pre-existing, not introduced.** It is also why `driveLiveFailbackOpening` deliberately does not re-arm the greeting's fatal no-speech deadline — so this gate, not that timer, is the proper fix. | A caller whose transfer already failed gets sent back to the same unanswered phone | `twilioStream.ts` | open |
| **P1 — `RICHA'S LINE` is frozen at session config** (`twilioStream.ts:621`) but re-checked at tool time (`:6393`). A call that CROSSES `TRANSFER_WINDOW_END` mid-conversation still lets the backend write a handoff the tool then refuses → promise-then-retract, which `backendRules.ts:105` forbids by name. `3192e65` shrank this from "every evening call" to "a call spanning 20:00"; the real fix refreshes the fact mid-call (`session.update` / `queueBackendUpdate`). | Residual of review finding F1 | `twilioStream.ts` | open |
| **P2 — the ~9s silence after a failed transfer**, between the ringing stopping and Erica speaking again (15s dial + ~9s reconnect). A Polly `<Say>` is banned by prior owner decision (jarring mid-call voice switch — see `handleTransferToOwner`), so the only non-jarring cover is a pre-recorded marin-voice clip via `<Play>`. | Callers may hang up in the gap, losing the message the failback exists to capture | `routes/twilio.ts` | **needs Aryan's product call** |
| **P2** — price-ambiguity detection covers only literal word overlap in service names. Inherited gaps: true synonymy with no shared word, words trapped only inside parentheticals, and combo rows (`Brow Thread + Lip Thread`) which contribute no keys by design. No live-catalog miss currently known. | `ambiguousPriceKeys` | `livePrompts.ts` | open |
| **P2** — `canceled` (caller hangs up mid-ring) now raises `transfer-failed`. Deliberate: they asked for Richa and did not get her, with the literal `DialCallStatus` surfaced in `transferDialStatus` so the QA sweep can downweight it. Revisit if noisy. | `computeFlags` | `admin.ts` | open |
| **P3** — a talkative robocall can again run to `MAX_CALL_MINUTES` when it produces no spoken decline. Restored deliberately in `b995cee` (F4) rather than hang up wordlessly on a possibly mis-tagged human. Revisit only if token cost shows up. | token cost on spam calls | `twilioStream.ts` | open |
